/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
      },
      {
        protocol: 'https',
        hostname: 'res.cloudinary.com',
      },
      {
        protocol: 'https',
        hostname: 'web-apex.com',
      },
    ],
  },
  // Allow dev server to run smoothly
  reactStrictMode: true,
};

export default nextConfig;
